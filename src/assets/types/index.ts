export interface FormUI {
    state: boolean
    formTitle: string
}
export interface Button {
    buttonName: string
    buttonText: string
}
export interface Alert {
    alertType: boolean
    alertMessage: string
}
export interface ComboboxItem {
    id: number
    name: string
}
